# ci-full.yml — EXPENSIVE full suite, ONLY on push/merge to main.
# PRs get the fast checks (pr-checks.yml); the 3-OS build matrix, fuzz,
# benchmarks, wasm, and C-API build run here, when code actually lands.
# Keeps CI minutes low on PRs while main is always fully verified.
name: ci-full
on:
  push:
    branches: [main]
    paths-ignore: # doc-only / lint-config-only pushes skip the EXPENSIVE suite
      # (pr-checks still runs: it owns typos + lychee, i.e. it lints the docs themselves).
      - "docs/**"
      - "*.md"
      - "typos.toml"
      - ".typos.toml"
permissions: { contents: read }
env:
  # setup-rust-toolchain injects RUSTFLAGS=-D warnings when the var is UNSET
  # (CI log shows `_srt_NEW_RUSTFLAGS: -D warnings`). Setting it explicitly,
  # even empty, disables that default — Cargo.toml [lints] levels govern
  # instead of a blanket warn->error promotion. Public passed with an empty
  # clippy section, so the promotion was never a deliberate standard.
  RUSTFLAGS: ""

jobs:
  # Rust-paths gate: bench + fuzz only run when Rust code (or their own harness /
  # workflow config) changed in the push. Docs/workflow-only pushes skip them;
  # pr-checks still lints every push. before/sha (not HEAD~1) so multi-commit
  # pushes are covered; all-zero before (new branch) defaults to run (safe).
  changes:
    runs-on: ubuntu-latest
    outputs:
      rust: ${{ steps.diff.outputs.rust }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 0
      - id: diff
        shell: bash
        run: |
          before="${{ github.event.before }}";
          if [[ "$before" =~ ^0+$ ]]; then
            echo "rust=true" >> "$GITHUB_OUTPUT"
          elif git diff --name-only "$before" "${{ github.sha }}" -- 'crates/**' 'Cargo.toml' 'Cargo.lock' 'fuzz/**' 'rust-toolchain.toml' 'tools/check-benchmark-regression.sh' '.github/**' | grep -q .; then
            echo "rust=true" >> "$GITHUB_OUTPUT"
          else
            echo "rust=false" >> "$GITHUB_OUTPUT"
          fi
  build:
    needs: [changes]
    if: needs.changes.outputs.rust == 'true'
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: actions-rust-lang/setup-rust-toolchain@166cdcfd11aee3cb47222f9ddb555ce30ddb9659 # v1
        with:
          target: ${{ matrix.os == 'macos-latest' && 'aarch64-apple-darwin' || '' }}
          cache: true
          cache-key: "rust-${{ matrix.os }}-${{ hashFiles('rust-toolchain.toml') }}-${{ hashFiles('**/Cargo.lock') }}"
      - uses: taiki-e/install-action@7b8d4719ee4aaa279bdf55df38dacb9ebfe12a6c # v2.87.6
        with:
          tool: nextest@0.9.143
      - name: Build
        shell: bash
        run: |
          exclude=""
          if [ "${{ matrix.os }}" = "macos-latest" ]; then exclude='--exclude ithmb-python'; fi
          cargo build --workspace $exclude
      - name: Build with logging feature
        shell: bash
        run: cargo build --features logging -p ithmb-core
      - name: Test
        shell: bash
        env:
          RUST_BACKTRACE: 1
        run: |
          exclude=""
          if [ "${{ matrix.os }}" = "macos-latest" ]; then exclude='--exclude ithmb-python'; fi
          cargo nextest run --workspace $exclude

  benchmark_regression:
    needs: [changes]
    if: needs.changes.outputs.rust == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: actions-rust-lang/setup-rust-toolchain@166cdcfd11aee3cb47222f9ddb555ce30ddb9659 # v1
        with:
          cache: true
          cache-key: "bench-${{ hashFiles('rust-toolchain.toml') }}-${{ hashFiles('.github/baseline.json') }}-${{ hashFiles('**/Cargo.lock') }}"
      - name: Benchmark regression check
        env:
          FAIL_THRESHOLD: 1.25
        shell: bash
        run: |
          tools/check-benchmark-regression.sh
      - name: Upload CI benchmark data
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: ci-benchmark-data
          path: /tmp/ci-benchmark-output.txt
          retention-days: 7
  fuzz:
    needs: [changes]
    if: needs.changes.outputs.rust == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: actions-rust-lang/setup-rust-toolchain@166cdcfd11aee3cb47222f9ddb555ce30ddb9659 # v1
        with:
          cache: true
          cache-key: "fuzz-${{ hashFiles('rust-toolchain.toml') }}-${{ hashFiles('fuzz/Cargo.toml') }}-${{ hashFiles('**/Cargo.lock') }}"
      - name: Install cargo-fuzz
        run: cargo install cargo-fuzz --debug --locked --version 0.13.2 2>&1 | tail -3
      - name: Fuzz decode_ithmb
        run: cargo fuzz run fuzz_decode_ithmb -- -max_total_time=30 2>&1 | tail -5
        working-directory: ./fuzz
      - name: Fuzz open_ithmb
        run: cargo fuzz run fuzz_open_ithmb -- -max_total_time=30 2>&1 | tail -5
        working-directory: ./fuzz
      - name: Fuzz encode roundtrip
        run: cargo fuzz run fuzz_encode_roundtrip -- -max_total_time=30 2>&1 | tail -5
        working-directory: ./fuzz
      - name: Fuzz decode_pipeline
        run: cargo fuzz run fuzz_decode_pipeline -- -max_total_time=30 2>&1 | tail -5
        working-directory: ./fuzz
      - name: Fuzz parse_photodb
        run: cargo fuzz run fuzz_parse_photodb -- -max_total_time=30 2>&1 | tail -5
        working-directory: ./fuzz
      - name: Fuzz parse_profile
        run: cargo fuzz run fuzz_parse_profile -- -max_total_time=30 2>&1 | tail -5
        working-directory: ./fuzz
      - name: Upload fuzz crash artifacts (reproducers)
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: fuzz-crashes
          path: fuzz/artifacts/
          retention-days: 14

  build_c_api:
    needs: [changes]
    if: needs.changes.outputs.rust == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: actions-rust-lang/setup-rust-toolchain@166cdcfd11aee3cb47222f9ddb555ce30ddb9659 # v1
        with:
          cache: true
          cache-key: "c-api-${{ hashFiles('rust-toolchain.toml') }}-${{ hashFiles('**/Cargo.lock') }}"
      - name: Build with c feature
        run: cargo build -p ithmb-core --features c
      - name: Verify C symbols are exported
        run: nm -D target/debug/libithmb_core.so | grep -E 'ithmb_decode|ithmb_prefix_to_profile'
      - uses: taiki-e/install-action@7b8d4719ee4aaa279bdf55df38dacb9ebfe12a6c # v2.87.6
        with:
          tool: nextest@0.9.143
      - name: Run C API integration test
        run: cargo nextest run -p ithmb-core --features c --test c_api_test

  wasm:
    needs: [changes]
    if: needs.changes.outputs.rust == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: actions-rust-lang/setup-rust-toolchain@166cdcfd11aee3cb47222f9ddb555ce30ddb9659 # v1
        with:
          target: wasm32-unknown-unknown
          cache: true
          cache-key: "wasm-${{ hashFiles('rust-toolchain.toml') }}-${{ hashFiles('crates/ithmb-wasm/Cargo.toml') }}"
      - name: Install wasm-pack
        shell: bash
        run: |
          # Pinned prebuilt musl binary (see ADR-0008). Keep the toolchain + wasm32
          # target above because wasm-pack shells out to cargo to build.
          curl -sSL https://github.com/rustwasm/wasm-pack/releases/download/v0.12.1/wasm-pack-v0.12.1-x86_64-unknown-linux-musl.tar.gz -o /tmp/wasm-pack.tgz
          tar xzf /tmp/wasm-pack.tgz -C /tmp --strip-components=1
      - name: Build wasm
        run: /tmp/wasm-pack build crates/ithmb-wasm/
